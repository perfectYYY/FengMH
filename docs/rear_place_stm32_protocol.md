# 已废弃：后放置 STM32 对接协议

本文件不再维护，已由 [后放置上下位机联调规范（steer）](rear_place_upper_lower_integration.md) 取代。

正式规范使用独立的 `REAR_PLACE=5`，并定义 blue、gray 两个方块的完整联调时序。请勿再依据本文件或
`rear_place_stm32_protocol(1).md` 实现“后放置复用 ARM 模式”的旧约定。
